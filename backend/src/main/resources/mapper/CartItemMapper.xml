<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eshop.mapper.CartItemMapper">

    <select id="selectCartByUserId" resultType="com.eshop.vo.CartVO">
        SELECT ci.id, ci.product_id, ci.quantity, ci.selected, ci.created_at,
               p.name AS product_name, p.main_image AS product_image,
               p.price, p.stock
        FROM cart_item ci
        INNER JOIN product p ON ci.product_id = p.id AND p.deleted = 0
        WHERE ci.user_id = #{userId}
        ORDER BY ci.created_at DESC
    </select>

</mapper>
